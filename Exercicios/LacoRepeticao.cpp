#include <stdio.h>

int main(){
	
	 
    float numeros[15]; // Array para armazenar os 15 números
    int i;

    // Laço para ler os 15 números
    for(i = 0; i < 15; i++) {
        printf("Digite o número %d: ", i+1);
        scanf("%f", &numeros[i]);
    }
    
    
    
    
}
